package now.link.mastigias.ui.library

enum class LibrarySortOrder {
    TITLE,
    ARTIST,
    ALBUM,
    DATE_MODIFIED,
    DATE_CREATED
}

enum class SortDirection {
    ASCENDING,
    DESCENDING
}
